{% extends 'base.html.twig' %}

{% block title %}CompanyType index{% endblock %}

{% block body %}
    <h1>CompanyType index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Name</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for company_type in company_types %}
            <tr>
                <td>{{ company_type.id }}</td>
                <td>{{ company_type.name }}</td>
                <td>
                    <a href="{{ path('company_type_show', {'id': company_type.id}) }}">show</a>
                    <a href="{{ path('company_type_edit', {'id': company_type.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="3">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('company_type_new') }}">Create new</a>
{% endblock %}
