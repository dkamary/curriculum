{% extends 'base.html.twig' %}

{% block title %}CompanyType{% endblock %}

{% block body %}
    <h1>CompanyType</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ company_type.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ company_type.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('company_type_index') }}">back to list</a>

    <a href="{{ path('company_type_edit', {'id': company_type.id}) }}">edit</a>

    {{ include('company_type/_delete_form.html.twig') }}
{% endblock %}
