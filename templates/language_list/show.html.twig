{% extends 'base.html.twig' %}

{% block title %}LanguageList{% endblock %}

{% block body %}
    <h1>LanguageList</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ language_list.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ language_list.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('language_list_index') }}">back to list</a>

    <a href="{{ path('language_list_edit', {'id': language_list.id}) }}">edit</a>

    {{ include('language_list/_delete_form.html.twig') }}
{% endblock %}
