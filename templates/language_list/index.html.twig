{% extends 'base.html.twig' %}

{% block title %}LanguageList index{% endblock %}

{% block body %}
    <h1>LanguageList index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Name</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for language_list in language_lists %}
            <tr>
                <td>{{ language_list.id }}</td>
                <td>{{ language_list.name }}</td>
                <td>
                    <a href="{{ path('language_list_show', {'id': language_list.id}) }}">show</a>
                    <a href="{{ path('language_list_edit', {'id': language_list.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="3">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('language_list_new') }}">Create new</a>
{% endblock %}
