{% extends 'base.html.twig' %}

{% block title %}SkillLevel{% endblock %}

{% block body %}
    <h1>SkillLevel</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ skill_level.id }}</td>
            </tr>
            <tr>
                <th>Value</th>
                <td>{{ skill_level.value }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ skill_level.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('skill_level_index') }}">back to list</a>

    <a href="{{ path('skill_level_edit', {'id': skill_level.id}) }}">edit</a>

    {{ include('skill_level/_delete_form.html.twig') }}
{% endblock %}
