<form method="post" action="{{ path('asset_type_delete', {'id': asset_type.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ asset_type.id) }}">
    <button class="btn">Delete</button>
</form>
