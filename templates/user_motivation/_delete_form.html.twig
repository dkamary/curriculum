<form method="post" action="{{ path('user_motivation_delete', {'id': user_motivation.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ user_motivation.id) }}">
    <button class="btn">Delete</button>
</form>
