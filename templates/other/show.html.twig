{% extends 'base.html.twig' %}

{% block title %}Other{% endblock %}

{% block body %}
    <h1>Other</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ other.id }}</td>
            </tr>
            <tr>
                <th>CreatedAt</th>
                <td>{{ other.createdAt ? other.createdAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>UpdatedAt</th>
                <td>{{ other.updatedAt ? other.updatedAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('other_index') }}">back to list</a>

    <a href="{{ path('other_edit', {'id': other.id}) }}">edit</a>

    {{ include('other/_delete_form.html.twig') }}
{% endblock %}
