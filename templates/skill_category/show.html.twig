{% extends 'base.html.twig' %}

{% block title %}SkillCategory{% endblock %}

{% block body %}
    <h1>SkillCategory</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ skill_category.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ skill_category.name }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ skill_category.description }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('skill_category_index') }}">back to list</a>

    <a href="{{ path('skill_category_edit', {'id': skill_category.id}) }}">edit</a>

    {{ include('skill_category/_delete_form.html.twig') }}
{% endblock %}
